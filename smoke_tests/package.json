{
  "dependencies": {
    "artillery": "1.6",
    "artillery-plugin-expect": "^1.3.0",
    "artillery-plugin-metrics-by-endpoint": "^0.1.1",
    "aws-sdk": "^2.903.0",
    "client-oauth2": "^4.3.3"
  },
  "scripts": {
    "smokeTestCatalogueApiStage": "artillery run -e stage catalogueApi.yaml",
    "smokeTestCatalogueApiProd": "artillery run -e prod catalogueApi.yaml"
  }
}

{
  "bool": {
    "should": [
      {
        "sparse_vector": {
          "field": "query.titleSemantic",
          "query": "{{query}}",
          "inference_id": "{{semanticModelId}}"
        }
      },
      {
        "sparse_vector": {
          "field": "query.descriptionSemantic",
          "query": "{{query}}",
          "inference_id": "{{semanticModelId}}"
        }
      }
    ]
  }
}
